<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.brooks.mall.user.mapper.ExpenseMapper">

    <insert id="insert">
        INSERT INTO expense_record (id, created_at, created_by, amount, category, expense_date, remark, payment_method)
        VALUES (#{id}, #{createdAt}, #{createdBy}, #{amount}, #{category}, #{expenseDate}, #{remark}, #{paymentMethod})
    </insert>
</mapper>